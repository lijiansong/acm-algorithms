#include <iostream>
#include <cstdio>
using namespace std;
const int MAX_N=101;
int weight[MAX_N]={0},value[MAX_N]={0},n,max_w;
//choose the part that total weight less than w from the i-th package
int choose(int i,int w)
{
	
}
void solve()
{
	
}
int main()
{
	while (cin>>n&&n)
	{
		for(int i=0;i<n;++i)
			cin>>weight[i]>>value[i];
		cin>>max_w;
		solve();
	}
	return 0;
}
